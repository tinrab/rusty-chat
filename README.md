# Rusty Chat

<p align="center">
  <img src="./cover.jpg"/>
</p>

...

## Set up

Run backend service:

```
$ RUST_LOG=info cargo run
```

And frontend:

```
$ cd frontend && nvm use && npm install
$ npm run start
```
